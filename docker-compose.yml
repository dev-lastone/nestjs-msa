version: '3.1'

services:
  user:
    build:
      context: .
      dockerfile: ./apps/user/Dockerfile
    command: pnpm run start:dev user
    ports:
      - '3001:3001'
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules